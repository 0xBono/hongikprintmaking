{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "yarn install; lerna bootstrap;",
    "package:pages": "lerna run --scope pages-module --stream dev",
    "package:render": "lerna run --scope render-module --stream start",
    "start": "run-p --print-label package:*",
    "build": "lerna run --parallel build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
